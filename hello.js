
var s1 = `dengan adanya node.js, \n` +
        `javascript dapat digunakan untuk membuat \n` +
        `Beragam tipe aplikasi`;

        var s2 = `dengan adanya Node.js,
        javascript dapat digunakan untuk
        membuat beragam tipe aplikasi` ;

        console.log(` s1 ` + s1);
        console.log(`\ns2 :` + s2);